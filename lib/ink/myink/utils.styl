hide()
    opacity 0

suppress()
    display none

italics()
    font-style italic

bold()
    font-weight bold

roman()
    font-weight normal

text-case(case)
    if case is 'up'
        case = uppercase
    else if case is 'low'
        case = lowercase
    else if case is 'capital'
        caes = capitalize

    text-transform case

lowercase()
    text-transform lowercase

capitalize()
    text-transform capitalize

uppercase()
    text-transform uppercase


typo(size,line_height=size+2)
    font-size size
    line-height line_height





//COMBOS

stoutText()
    uppercase()
    bold()








//SIDES


getPushSide(orient)
    if orient is vertical
        return vertical_push_side
    else if orient is horizontal
        return horizontal_push_side

getStart(orient)
    if orient is vertical
        top
    else if orient is horizontal
        left

getEnd(orient)
    if orient is vertical
        bottom
    else if orient is horizontal
        right

//TODO support corners like down right
oppositeSide(side)
    side = directionToSide(side)
    opposite-position(side)

//TODO opposite limiit
limitToSide(orient,limit)
    if orient is vertical
        if limit is start
            top
        else if limit is end
            bottom
    else if orient is horizontal
        if limit is start
            left
        else if limit is end
            right

directionToSide(direction)
    if direction is up
        top
    else if direction is down
        bottom
    else
        direction
    
//TODO implement a `by` feature, so I can `clockwise` up by 3 and get `right`
//TODO support corner turning, like going from "bottom left" to "bottom right" to "top left" etc.
clockwiseSide(side)
    side = directionToSide(side)
    if side is top
        right
    else if side is right
        bottom
    else if side is bottom
        left
    else if side is left
        top

counterClockwiseSide(side)
    side = directionToSide(side)
    if side is top
        left
    else if side is left
        bottom
    else if side is bottom
        right
    else if side is right
        top

